{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Wait for API :8000",
      "type": "shell",
      "options": {
        "shell": {
          "executable": "C:\\Windows\\System32\\cmd.exe",
          "args": ["/d", "/c"]
        }
      },
      "command": "for /l %i in (0,0,1) do (curl -s http://127.0.0.1:8000 -o NUL && exit /b 0 || timeout /t 1 >NUL)",
      "problemMatcher": []
    }
  ]
}
